# ============================================================================
# DATASET PIPELINE REQUIREMENTS
# ============================================================================
# Python version: 3.10.11 (as per your notebook)
#
# Installation:
#   pip install -r requirements.txt
#
# Optional (for Windows Poppler support):
#   Download Poppler from: https://github.com/oschwartz10612/poppler-windows/releases/
#   Extract to: C:\Program Files\poppler\
# ============================================================================

# Core dependencies
pathlib2==2.3.7.post1           # For pathlib compatibility (if needed)

# PDF Processing
PyPDF2==3.0.1                   # PDF reading
PyMuPDF==1.23.8                 # Main PDF text extraction (fitz)
pdf2image==1.16.3               # PDF to image conversion (requires Poppler)

# OCR Dependencies
paddleocr==2.7.0.3              # OCR engine (replaces Tesseract)
paddlepaddle==2.6.2             # PaddlePaddle backend for OCR (updated from 2.5.2)
opencv-python==4.8.1.78         # Required by PaddleOCR
shapely==2.0.2                  # Required by PaddleOCR

# NLP & Text Processing
nltk==3.8.1                     # Natural Language Toolkit
symspellpy==6.7.7               # Spell checking (optional but recommended)

# Data Processing
pandas==2.1.4                   # Data manipulation
numpy==1.26.2                   # Numerical operations (required by pandas)

# Utilities
Pillow==10.1.0                  # Image processing (required by pdf2image)

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================
# These are optional but recommended for full functionality:

# For better spell checking (requires dictionary file)
# symspellpy==6.7.7

# For advanced NLP tasks
# spacy==3.7.2

# ============================================================================
# NOTES:
# ============================================================================
# 1. PaddleOCR requires additional downloads on first run (models)
# 2. NLTK may require additional data downloads (punkt, stopwords)
# 3. Poppler is system-level dependency for pdf2image
# 4. On Windows, ensure Visual C++ redistributables are installed
# ============================================================================